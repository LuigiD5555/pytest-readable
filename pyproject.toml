[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pytest-specview"
version = "0.1.0"
description = "Interactive natural-language spec viewer for pytest projects"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[project.scripts]
specview = "pytest_specview.cli:main"
specview-compile-locales = "pytest_specview.compile_locales:main"

[tool.setuptools.packages.find]
include = ["pytest_specview*"]

[tool.setuptools.package-data]
pytest_specview = ["locale/*/LC_MESSAGES/*.po", "locale/*/LC_MESSAGES/*.mo"]
